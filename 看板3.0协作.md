# 看板3.0协作

## 1. 文档目的

这份文档用于固定 `weekly-review-dashboard/3.0` 的协作方式，覆盖：

- 本地数据从哪里来
- DataGrip 里怎么查
- 本地数据库怎么更新
- 线上 Netlify 应该怎么上传
- 后续如何把工作交给另一个对话继续

这不是产品说明书，而是操作手册。

---

## 2. 当前结论

当前 3.0 的数据协作已经拆成两层：

1. 本地 SQLite
   - 用于查数、对数、补字段、做宽表、做回溯
   - 给 DataGrip 使用
2. Netlify 线上缓存
   - 用于线上看板读取
   - 不建议让线上直接连数仓

推荐路径不是“每次手工改前端数据”，而是：

```text
数仓 / 飞书表
  -> 本地同步脚本
  -> 本地 SQLite
  -> 导出看板读模型
  -> 上传到 Netlify Blobs
  -> 线上看板读取
```

---

## 3. 关键路径

### 3.1 项目路径

- 看板 3.0：
  `D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0`
- DataGrip 工程：
  `C:\Users\HP\DataGripProjects\数仓`
- 本地 SQLite：
  `C:\Users\HP\DataGripProjects\数仓\local_cache\weekly_review_cache.sqlite`
- 定时任务日志目录：
  `C:\Users\HP\DataGripProjects\weekly_review_cache_logs`

### 3.2 关键脚本

- 本地同步主脚本：
  [sync-local-review-db.js](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/sync-local-review-db.js)
- 数仓日更任务执行脚本：
  [run-local-review-warehouse-sync.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/run-local-review-warehouse-sync.ps1)
- 站外 / PR 周更任务执行脚本：
  [run-local-review-offsite-sync.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/run-local-review-offsite-sync.ps1)
- 安装数仓日更任务：
  [install-local-review-warehouse-task.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/install-local-review-warehouse-task.ps1)
- 安装站外 / PR 周更任务：
  [install-local-review-offsite-task.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/install-local-review-offsite-task.ps1)
- 上传前本地校验：
  [validate-local-review-upload.js](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/validate-local-review-upload.js)
- 本地 SQLite 构建上传包：
  [local-review-cache-package.js](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/local-review-cache-package.js)
- 上传前校验任务执行脚本：
  [run-local-review-upload-precheck.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/run-local-review-upload-precheck.ps1)
- 本地 SQLite 上传到 Netlify 执行脚本：
  [run-local-review-netlify-upload.ps1](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/scripts/run-local-review-netlify-upload.ps1)
- 飞书 / 数仓读取服务：
  [offsite-lark-sync-server.js](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/offsite-lark-sync-server.js)
- Netlify 上传说明：
  [NETLIFY_WAREHOUSE_SYNC.md](D:/projects/nail-attribution-console-demo/output/weekly-review-dashboard/3.0/NETLIFY_WAREHOUSE_SYNC.md)

---

## 4. 数据来源

### 4.1 帖子数据

帖子数据来自数仓，通过 `offsite-lark-sync-server.js` 中的数仓查询函数拉取，再写入本地 SQLite。

当前本地库里有三层帖子表：

- `warehouse_posts`
  - 帖子主表
  - 一条帖子一行
  - 存标题、链接、平台、渠道、负责人、项目、品线、主题、形式等

- `warehouse_post_snapshots`
  - 帖子每日快照表
  - 一条帖子在某抓取日的一组指标一行
  - 存曝光、点赞、评论、分享、收藏、互动

- `warehouse_post_wide`
  - 帖子完整宽表
  - 一行就是“某帖子在某抓取日的完整明细”
  - 这是后续最适合直接查和做导出的表

### 4.2 站外数据

站外数据来自飞书表，通过 `buildDtcSection()` 拉取并落地到本地 SQLite。

当前本地库里有两张站外表：

- `offsite_rows`
  - 独立站 / TikTok Shop 等表格明细
- `offsite_pr_metrics`
  - PR 三个卡片指标
  - `社交媒体提及量`
  - `Media提及量`
  - `SOV（Pending）`

### 4.3 同步日志

- `sync_runs`
  - 记录每次全量或增量同步
  - 看是否成功、用了什么范围、结束时间是什么

---

## 5. DataGrip 怎么查

### 5.1 在哪里看

打开 DataGrip 的 `数仓` 工程，在数据库工具窗看：

- `readonly_ai warehouse`
- `weekly_review_cache_local`

本地库在：

```text
weekly_review_cache_local -> main
```

### 5.2 主要查哪张表

如果只是想查当前帖子集合，优先查：

- `warehouse_post_wide`

如果想看帖子基础资料，查：

- `warehouse_posts`

如果想看某条帖子每天怎么变化，查：

- `warehouse_post_snapshots`

如果想看站外表格，查：

- `offsite_rows`
- `offsite_pr_metrics`

### 5.3 常用 SQL

#### 看宽表总量

```sql
SELECT COUNT(*) AS row_count
FROM warehouse_post_wide;
```

#### 看最近抓取的帖子明细

```sql
SELECT
  captured_at,
  publish_date,
  platform,
  channel_type,
  channel_name,
  project,
  product_line_1,
  content_topic,
  content_format,
  title,
  exposure,
  interaction
FROM warehouse_post_wide
ORDER BY captured_at DESC, publish_date DESC
LIMIT 200;
```

#### 看某个复盘周的 PR 指标

```sql
SELECT
  review_start,
  review_end,
  label,
  value_text,
  format_type,
  updated_at
FROM offsite_pr_metrics
WHERE review_start = '2026-05-18'
  AND review_end = '2026-05-24'
ORDER BY label;
```

#### 看最近同步状态

```sql
SELECT
  id,
  sync_type,
  status,
  warehouse_range_start,
  warehouse_range_end,
  offsite_anchor_start,
  offsite_anchor_end,
  started_at,
  finished_at
FROM sync_runs
ORDER BY id DESC
LIMIT 20;
```

---

## 6. 本地怎么更新数据

### 6.1 默认全量同步

主张拆开执行，不再默认把帖子数仓和飞书站外绑在同一个计划任务里。当前默认拉库口径为全量：帖子数仓全量拉取后重建本地宽表，站外 / PR 全量回补当前配置窗口。

#### 日常全量更新帖子数仓

```powershell
cd D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0
node --no-warnings .\scripts\sync-local-review-db.js --full --warehouse-only
```

作用：

- 全量重拉当前数仓范围
- 自动重建 `warehouse_post_wide`

#### 周更全量更新站外 / PR

```powershell
cd D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0
node --no-warnings .\scripts\sync-local-review-db.js --full --offsite-only
```

作用：

- 回补最近 `12` 个延后一周的站外复盘周
- 更新 `offsite_rows`
- 更新 `offsite_pr_metrics`

#### 兼容型合并全量

只有在你明确想一次把两块都一起刷新时，再用：

```powershell
cd D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0
node --no-warnings .\scripts\sync-local-review-db.js --full
```

作用：

- 全量重拉当前数仓范围
- 回补最近 `12` 个延后一周的站外复盘周
- 自动重建 `warehouse_post_wide`

### 6.2 增量同步仅作临时调试

只有在临时排查性能、缩短回刷窗口时，才手动使用增量参数：

```powershell
cd D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0
node --no-warnings .\scripts\sync-local-review-db.js --incremental
```

作用：

- 回刷最近 `35` 天帖子数据
- 回刷最近 `4` 个延后一周的站外复盘周
- 自动重建 `warehouse_post_wide`

### 6.3 已创建的计划任务

当前正式使用两条任务：

- `WeeklyReviewDashboard_LocalWarehouseSync`
  - 每天 `09:00`
  - 只更新帖子数仓 / 宽表
- `WeeklyReviewDashboard_LocalOffsiteSync`
  - 每周五 `19:00`
  - 只更新站外 / PR

旧的合并任务 `WeeklyReviewDashboard_LocalReviewDbSync` 已删除，避免重复跑旧逻辑。

为周五线上上传链路预留两条新增任务：

- `WeeklyReviewDashboard_LocalUploadPrecheck`
  - 每周五 `19:45`
  - 只做上传前本地校验，失败时写报告并返回非 0 退出码
- `WeeklyReviewDashboard_LocalNetlifyUpload`
  - 每周五 `20:00`
  - 从本地 SQLite 构建上传包并上传 Netlify
  - 上传脚本会再次运行同一套确定性校验；校验失败直接阻止上传

周五如果要在上传前再补跑一次帖子数仓，可用新版安装脚本给 `WeeklyReviewDashboard_LocalWarehouseSync` 增加周五 `19:30` 触发器。

查看任务：

```powershell
schtasks /Query /TN "WeeklyReviewDashboard_LocalWarehouseSync" /V /FO LIST
schtasks /Query /TN "WeeklyReviewDashboard_LocalOffsiteSync" /V /FO LIST
```

查看日志：

```powershell
Get-Content C:\Users\HP\DataGripProjects\weekly_review_cache_logs\daily-warehouse-sync.log -Tail 100
Get-Content C:\Users\HP\DataGripProjects\weekly_review_cache_logs\weekly-offsite-sync.log -Tail 100
Get-Content C:\Users\HP\DataGripProjects\weekly_review_cache_logs\pre-upload-validation.log -Tail 100
Get-Content C:\Users\HP\DataGripProjects\weekly_review_cache_logs\weekly-netlify-upload.log -Tail 100
```

---

## 7. 如何更新看板 3.0

这里要分清两件事。

### 7.1 更新本地数据库

只跑本地同步脚本就行。  
这一步已经能让你在 DataGrip 里查到最新数据。

### 7.2 更新线上 Netlify 看板

只更新本地数据库还不够。  
线上看板不会自动读你的本地 SQLite。

线上推荐流程是：

```text
本地同步 SQLite
  -> 上传前本地校验
  -> 从 SQLite 导出看板读模型
  -> 上传到 Netlify Blobs
  -> 线上看板读取新的缓存包
```

也就是说：

- 本地库是“数据中台 / 审计库”
- 线上缓存包才是“线上看板数据源”
- 上传阶段不再重新直连数仓或飞书，线上上传的数据源统一为本地 SQLite

---

## 8. Netlify 线上推荐方案

推荐做法是“定时上传数据包”，不是上传整个 `.sqlite` 文件。

### 8.1 为什么不传整个数据库文件

不推荐把 `weekly_review_cache.sqlite` 直接给线上看板使用，因为：

- 前端不能直接稳定读取 SQLite 文件
- 包太重，不适合页面加载
- 线上只需要读模型，不需要把整库暴露出去

### 8.2 当前上传的数据包

当前已落地的是兼容线上 `/api/posts/warehouse-cache?full=1` 的全量包：

- `posts/full.json`
  - 全量帖子主数据
  - 全量帖子快照
  - 最新站外 / PR 复盘窗口
  - 给帖子明细、渠道诊断、总览卡片、站外、PR 卡片使用

后续如果要进一步拆包，再拆成以下几类：

- `posts_wide_latest.json.gz`
  - 每个帖子只保留最新快照
  - 给轻量帖子明细、渠道诊断、总览卡片使用

- `posts_snapshots_recent.json.gz`
  - 最近若干周的帖子快照
  - 给 cohort、趋势、生命周期使用

- `offsite_rows.json.gz`
  - 站外明细

- `offsite_pr_metrics.json.gz`
  - 站外 PR 指标卡

- `manifest.json`
  - 记录版本号、更新时间、范围、行数、hash

### 8.3 为什么推荐本地上传到 Netlify

因为本地环境已经能稳定拿到：

- 数仓
- 飞书
- 本地 SQLite

而 Netlify 不一定稳定具备这些访问条件。  
所以更稳的方式是：

```text
本地电脑 = 数据代理
Netlify = 只读缓存层
```

### 8.4 线上同步的推荐顺序

1. 周五 `19:00` 跑 `WeeklyReviewDashboard_LocalOffsiteSync`
2. 周五 `19:30` 可补跑 `WeeklyReviewDashboard_LocalWarehouseSync`
3. 周五 `19:45` 跑 `WeeklyReviewDashboard_LocalUploadPrecheck`
4. 周五 `20:00` 跑 `WeeklyReviewDashboard_LocalNetlifyUpload`
5. 上传脚本从本地 SQLite 生成看板专用数据包
6. 上传到 Netlify Blobs
7. 线上看板读取新的缓存

上传前校验至少覆盖：

- 最近帖子数仓同步是否成功
- 最近站外 / PR 同步是否成功
- `warehouse_post_wide` 最新宽表是否有数据且行数达到阈值
- `offsite_rows` 是否存在当前延后一周复盘窗口数据
- `offsite_pr_metrics` 的三张 PR 卡是否齐全
- 关键字段缺失率是否超过阈值
- 数据量是否相对上次通过校验异常波动
- 本地待上传数据包汇总值是否和 SQLite 计数一致

校验报告默认写入：

```text
C:\Users\HP\DataGripProjects\weekly_review_cache_logs\pre-upload-validation-latest.json
```

---

## 9. 后续交给另一个对话怎么说

后续如果要把工作交给另一个对话，最好不要只说“帮我继续”。  
要把这几个信息一起带上。

### 9.1 最小交接信息

至少说明：

- 项目路径
  - `D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0`
- DataGrip 工程路径
  - `C:\Users\HP\DataGripProjects\数仓`
- 本地库路径
  - `C:\Users\HP\DataGripProjects\数仓\local_cache\weekly_review_cache.sqlite`
- 当前帖子宽表
  - `warehouse_post_wide`
- 当前站外表
  - `offsite_rows`
  - `offsite_pr_metrics`
- 定时任务名
  - `WeeklyReviewDashboard_LocalWarehouseSync`
  - `WeeklyReviewDashboard_LocalOffsiteSync`
  - `WeeklyReviewDashboard_LocalUploadPrecheck`
  - `WeeklyReviewDashboard_LocalNetlifyUpload`
- 目标是本地库还是线上 Netlify

### 9.2 推荐交接话术

可以直接复制这段给下一个对话：

```text
请基于 D:\projects\nail-attribution-console-demo\output\weekly-review-dashboard\3.0 继续处理。

当前已经有本地 SQLite 协作层：
- 库文件：C:\Users\HP\DataGripProjects\数仓\local_cache\weekly_review_cache.sqlite
- 帖子宽表：warehouse_post_wide
- 站外表：offsite_rows / offsite_pr_metrics
- 同步日志表：sync_runs

本地计划任务已创建：
- WeeklyReviewDashboard_LocalWarehouseSync
- WeeklyReviewDashboard_LocalOffsiteSync

线上上传链路任务（启用前需确认）：
- WeeklyReviewDashboard_LocalUploadPrecheck
- WeeklyReviewDashboard_LocalNetlifyUpload

DataGrip 工程：
- C:\Users\HP\DataGripProjects\数仓

请先读取：
- 3.0\看板3.0协作.md
- 3.0\NETLIFY_WAREHOUSE_SYNC.md

目标是：
[这里补你当次的明确任务]
```

### 9.3 如果是让下一个对话更新线上

额外再补：

- 当前线上站点 URL
- 是否允许更新生产
- 是否先只做本地验证
- 是否启用默认全量上传任务链

---

## 10. 维护原则

后续继续做这套协作，建议坚持下面几条：

- 本地 SQLite 负责“真实查询和中间层沉淀”
- 线上只读缓存包，不直连数仓
- 先把口径在本地 DataGrip 查清，再更新看板
- 改飞书 / PR / 数仓字段后，先跑本地同步再验证
- 需要交接时，优先让下一个对话先读这份文档

---

## 11. 当前可用状态

截至当前，这套协作已经具备：

- 本地帖子主表
- 本地帖子快照表
- 本地帖子宽表
- 本地站外明细表
- 本地 PR 指标表
- 本地同步日志表
- 每天 09:00 自动同步帖子数仓
- 每周五 19:00 自动同步站外 / PR
- 已具备上传前本地校验脚本
- 已具备本地 SQLite 到 Netlify 上传脚本
- DataGrip 可直接查询

下一步如果要继续演进，优先级建议是：

1. 从 SQLite 生成线上读模型包
2. 增加上传到 Netlify Blobs 的正式脚本
3. 把线上 3.0 看板改成优先读这套缓存包
